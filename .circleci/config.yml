version: 2
jobs:
   build:
     working_directory: ~/gradle-pitest-plugin
     docker:
       - image: circleci/openjdk:8-jdk
         environment:
           TERM: dumb
     steps:
       - checkout
       - run: |
         ./gradlew check
         ./gradlew funcTest

